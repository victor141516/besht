const words = ["apple", "banana", "apple", "orange", "banana", "apple"];

const counts = words.reduce((acc, word) => {
  if (!acc[word]) {
    acc[word] = 0
  }
  acc[word] = acc[word] + 1;
  return acc;
}, {});

console.log(counts);