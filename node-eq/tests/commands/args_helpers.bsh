let all = Besht.args.argv()
let first = Besht.args.positional(1) ?? "missing"
let second = Besht.args.positional(2) ?? "missing"
let third = Besht.args.positional(3) ?? "missing"
let missingPos = Besht.args.positional(9) ?? "missing"
let branch = Besht.args.option("branch", "b") ?? "main"
let emptyOpt = Besht.args.option("empty") ?? "fallback"
let missingOpt = Besht.args.option("missing") ?? "fallback"
let dryRun = Besht.args.flag("dry-run", "d")
let verbose = Besht.args.flag("verbose", "v")

console.log(`argv: ${all.join("|")}`)
console.log(`first: ${first}`)
console.log(`second: '${second}'`)
console.log(`third: ${third}`)
console.log(`missing positional: ${missingPos}`)
console.log(`branch: ${branch}`)
console.log(`empty option: '${emptyOpt}'`)
console.log(`missing option: ${missingOpt}`)
console.log(`dry run: ${dryRun}`)
console.log(`verbose: ${verbose}`)
